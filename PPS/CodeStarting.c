#include <stdio.h>
int main ()
{
    float a,b;
    printf("Enter a number - ");
    scanf("%f", &a);

    if (a==2)
    printf("%f is 1", a);

    else 
    printf("%f is not 1", a);

    return 0;
}